apiVersion: v1
kind: PersistentVolume
metadata:
  name: standard
  labels:
    type: local
spec:
  #storageClassName: manual
  capacity:
    storage: 10Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: /d/Git/GitHub/pgbouncer/disk